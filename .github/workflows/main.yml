name: Simple Workflow Check

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: "Run Hello World Step for 24f1002320@ds.study.iitm.ac.in"
        run: echo "Hello, world!"
